{
  "name": "findora-graph",
  "version": "1.0.0",
  "main": "README.md",
  "repository": "git@github.com:FindoraNetwork/findora-graph.git",
  "author": "Findora Foundation",
  "license": "MIT",
  "private": true,
  "scripts": {
    "gen": "graph codegen",
    "build": "graph build",
    "create:local": "graph create --node http://127.0.0.1:8020",
    "deploy:local": "graph deploy --product hosted-service --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001",
    "create": "graph create",
    "deploy": "graph deploy --product hosted-service"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.35.0",
    "@graphprotocol/graph-ts": "^0.28.1"
  }
}
